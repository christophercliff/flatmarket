#!/usr/bin/env node

console.error([
    'You have mistakenly installed the `flatmarket` package which is a no-op as of Flatmarket 3.',
    'Please install `flatmarket-cli` instead',
].join('\n'))
process.exit(1)
